apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cvmfs-buildbot-db-pv-claim
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: rbd-k8s
  resources:
    requests:
      storage: 12Gi
